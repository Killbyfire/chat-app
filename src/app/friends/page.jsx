// Add friend input with custom friend code
// get friends by using .lk (like) and check if the current user id is in anyones friends array
// custom nicknames stored like userid:name,userid:name

export default async function FriendsPage() {
    
}